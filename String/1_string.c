#include<stdio.h>
int main()
{
    char s1[20];
    char s2[10];
 printf("Enter the string:");
 scanf("%s",s1);
 printf("Enter the scound string:");
 scanf("%s",s2);
 printf("The first and scound string is %s %s",s1,s2);
 return 0;
}