#include <stdio.h>
#include <conio.h>

int main()
{
    int a =anish, b =ankan, c=sayan;
        printf(" Enter your choice ");
        scanf("%d %d %d"; &a %b &c);
        printf("show %d & %d & %d \n"; a, b, c );
        return(0);
}